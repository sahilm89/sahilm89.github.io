# Overrides for the preview deployment. GitHub serves project sites under the user site's custom
# domain, so the preview lives at https://www.sahilmoza.com/site-preview/
# Usage: bin/preview   (or: bundle exec jekyll build --config _config.yml,_config_preview.yml)
url: https://www.sahilmoza.com
baseurl: /site-preview
noindex: true
